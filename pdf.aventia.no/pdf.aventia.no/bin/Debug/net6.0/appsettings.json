{
  // Connection strings used by the application, typically for databases
  "ConnectionStrings": {
    // The default connection string to be used for connecting to the database
    "DefaultConnection": "server=localhost;user=pdf;password=AventiaPdfTesting123;database=pdf;AllowUserVariables=true;sslmode=none;AllowPublicKeyRetrieval=true"
  },

  // Logging settings for the application
  "Logging": {
    // Log level settings
    "LogLevel": {
      // The default level for logging information
      "Default": "Information",
      // The level for logging information from Microsoft.AspNetCore
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // A list of hosts that are allowed to access this application
  "AllowedHosts": "*"
}
